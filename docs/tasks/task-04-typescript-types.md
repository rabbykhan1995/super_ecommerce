# Task 04 — TypeScript Types (Copy from Web)

> **Phase**: 2 — Core Infrastructure
> **Say**: "generate task 4" or "generate task 04"

## Objective

Copy TypeScript type definitions from the web project with no changes needed.

## Source Files to Copy

From `/ecommerce/types/` → `mobile/types/`

| Source | Target | Changes |
|--------|--------|---------|
| `cart.types.ts` | `types/cart.types.ts` | None |
| `product.types.ts` | `types/product.types.ts` | None |
| `order.types.ts` | `types/order.types.ts` | None |
| `program.types.ts` | `types/program.types.ts` | None |

## Instructions

1. Read each source file from `/ecommerce/types/`
2. Copy verbatim to `mobile/types/`
3. No modifications required — types are shared between web and mobile

## Verify

Import each type file in a test component — TypeScript should compile without errors.
